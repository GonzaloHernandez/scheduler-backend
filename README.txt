SCHEDULER BACKEND

This package aim to offer database services to a Scheduler Frontend.


INSTALLATION

Previewsly you have to install [Node.js] (https://nodejs.org/).

Once this package have been cloned is required to install all Node dependencies, below command will do the work for you.

#npm install


USAGE

#npm start

http://localhost:3001/launchschema
